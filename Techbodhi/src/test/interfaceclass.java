package test;

public interface interfaceclass {
	int a =10; 
	//variable should be initialized at the time of declaration 
	//and will be public static final by default
	void abc(); //by default method will be public and abstract and all methods are abstract
}
